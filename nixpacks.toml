# Zyntra Software Lab (DEMO) — let Nixpacks Python provider own pip.
providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.12"
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"

# Extra pkgs only — do not replace the provider's Python+pip toolchain.
[phases.setup]
nixPkgs = ["gettext"]
